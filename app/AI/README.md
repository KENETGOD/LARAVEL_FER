# Application AI

The `app/AI/` directory contains only application AI logic that is part of the Laravel backend.

Create subdirectories only when real application code requires them. Do not add empty directories or speculative abstractions.
